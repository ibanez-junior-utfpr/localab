import { StyleSheet } from "react-native";

export const Estilo = StyleSheet.create({
    logo: {
        width: "auto",
        resizeMode: "contain",
        height: 200
    },
    container: {
        flex: 1
    }
});
